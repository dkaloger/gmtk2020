using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Plant was ready for too long and is dead now.
/// </summary>
public class RottenState : ByTheTale.StateMachine.State
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

}
